import Link from 'next/link';
import { FacebookIcon, InstagramIcon, XMarkIcon } from '@heroicons/react/outline';

export default function Footer() {
  return (
    <footer className="bg-aqua-blue text-white py-12">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {/* ... (usa el código del Footer que ya corregimos antes) */}
      </div>
    </footer>
  );
}
